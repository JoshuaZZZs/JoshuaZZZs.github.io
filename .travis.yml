language: node_js
node_js: stable
sudo: required # 需要管理员权限
# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "JoshuaZZZs"
  - git config user.email "15979089154@189.cn"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF:  github.com/JoshuaZZZs/JoshuaZZZs.github.io.git
